{
    "scripts": {
        "dev": "npm-run-all --parallel dev:admin-web dev:material",
        "dev:admin-web": "npm run dev --prefix admin-web",
        "dev:material": "npm run dev --prefix material"
    },
    "devDependencies": {
        "npm-run-all": "^4.1.5"
    }
}
